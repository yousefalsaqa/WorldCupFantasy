// Ambient module declarations for packages that ship JS only.

// HTML5 drag-and-drop polyfill for iOS / Android Safari. Imported purely
// for its side effect (registers a touchstart listener that synthesises
// drag events) – no exported API used.
declare module '@dragdroptouch/drag-drop-touch';
